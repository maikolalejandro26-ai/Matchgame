// ==================== TIENDA FUNCIONES ====================
window.abrirTienda=function(){document.getElementById('modalTienda').classList.remove('hidden');document.getElementById('puntosTienda').innerText=userActual?.puntos||0;mostrarSeccionTienda('marcos');};
window.abrirTiendaDesdeChat=function(){if(!chatActivo)return mostrarToast('Abre un chat primero','warning');abrirTienda();mostrarSeccionTienda('regalos');};
window.cerrarTienda=function(){document.getElementById('modalTienda').classList.add('hidden');};

window.mostrarSeccionTienda=function(s){seccionTiendaActual=s;['tabMarcos','tabColores','tabRegalos','tabFondos','tabEfectos','tabMascotas'].forEach(id=>{const el=document.getElementById(id);if(el){el.style.background=id===('tab'+s.charAt(0).toUpperCase()+s.slice(1))?'var(--primary)':'var(--btn-bg)';el.style.color=id===('tab'+s.charAt(0).toUpperCase()+s.slice(1))?'white':'var(--btn-text)';}});const div=document.getElementById('contenidoTienda');if(!div)return;if(s==='marcos'){div.innerHTML=marcosTienda.sort((a,b)=>a.precio-b.precio).map(m=>`<div style="display:flex;align-items:center;justify-content:space-between;padding:8px 6px;border-bottom:1px solid var(--border);"><div style="display:flex;align-items:center;gap:8px;"><div style="width:38px;height:38px;border-radius:50%;background:#e0e0e0;display:flex;align-items:center;justify-content:center;font-size:20px;" class="marco-${m.id}">${m.icono}</div><div><b>${m.nombre}</b><br><small>${m.precio} pts</small></div></div><button onclick="window.comprarMarco('${m.id}',${m.precio})" ${userActual?.puntos<m.precio?'disabled':''} style="width:auto;padding:5px 12px;font-size:11px;min-height:auto;background:var(--primary);color:white;border-radius:16px;">Comprar</button></div>`).join('');}else if(s==='colores'){div.innerHTML=coloresTienda.sort((a,b)=>a.precio-b.precio).map(c=>`<div style="display:flex;align-items:center;justify-content:space-between;padding:8px 6px;border-bottom:1px solid var(--border);"><div style="display:flex;align-items:center;gap:8px;"><div class="nombre-${c.id}" style="font-size:24px;font-weight:900;">Aa</div><div><b>${c.nombre}</b><br><small>${c.precio} pts</small></div></div><button onclick="window.comprarColor('${c.id}',${c.precio})" ${userActual?.puntos<c.precio?'disabled':''} style="width:auto;padding:5px 12px;font-size:11px;min-height:auto;background:var(--primary);color:white;border-radius:16px;">Comprar</button></div>`).join('');}else if(s==='regalos'){div.innerHTML=regalosTienda.sort((a,b)=>a.precio-b.precio).map(r=>`<div style="display:flex;align-items:center;justify-content:space-between;padding:8px 6px;border-bottom:1px solid var(--border);"><div style="display:flex;align-items:center;gap:8px;"><span style="font-size:26px;">${r.icono}</span><div><b>${r.nombre}</b><br><small>${r.precio} pts</small></div></div><button onclick="window.seleccionarMatchParaRegalo('${r.id}',${r.precio})" ${userActual?.puntos<r.precio?'disabled':''} style="width:auto;padding:5px 12px;font-size:11px;min-height:auto;background:#ff6b9d;color:white;border-radius:16px;">Enviar</button></div>`).join('');}else if(s==='fondos'){div.innerHTML=fondosTienda.sort((a,b)=>a.precio-b.precio).map(f=>`<div style="display:flex;align-items:center;justify-content:space-between;padding:8px 6px;border-bottom:1px solid var(--border);"><div style="display:flex;align-items:center;gap:8px;"><div style="width:38px;height:38px;border-radius:10px;background:linear-gradient(135deg,${f.color1},${f.color2});"></div><div><b>${f.nombre}</b><br><small>${f.precio} pts</small></div></div><button onclick="window.comprarFondo('${f.id}',${f.precio})" ${userActual?.puntos<f.precio?'disabled':''} style="width:auto;padding:5px 12px;font-size:11px;min-height:auto;background:var(--primary);color:white;border-radius:16px;">Comprar</button></div>`).join('');}else if(s==='efectos'){div.innerHTML=efectosTienda.sort((a,b)=>a.precio-b.precio).map(e=>`<div style="display:flex;align-items:center;justify-content:space-between;padding:8px 6px;border-bottom:1px solid var(--border);"><div style="display:flex;align-items:center;gap:8px;"><div style="font-size:26px;">✨</div><div><b>${e.nombre}</b><br><small>${e.precio} pts</small></div></div><button onclick="window.comprarEfecto('${e.id}',${e.precio})" ${userActual?.puntos<e.precio?'disabled':''} style="width:auto;padding:5px 12px;font-size:11px;min-height:auto;background:var(--primary);color:white;border-radius:16px;">Comprar</button></div>`).join('');}else if(s==='mascotas'){div.innerHTML=mascotasTienda.sort((a,b)=>a.precio-b.precio).map(m=>`<div style="display:flex;align-items:center;justify-content:space-between;padding:8px 6px;border-bottom:1px solid var(--border);"><div style="display:flex;align-items:center;gap:8px;"><span style="font-size:30px;">${m.icono}</span><div><b>${m.nombre}</b><br><small>${m.precio} pts</small></div></div><button onclick="window.comprarMascota('${m.id}',${m.precio})" ${userActual?.puntos<m.precio?'disabled':''} style="width:auto;padding:5px 12px;font-size:11px;min-height:auto;background:var(--primary);color:white;border-radius:16px;">Comprar</button></div>`).join('');}feather.replace();};

window.comprarMarco=async function(id,precio){if(!userActual||userActual.puntos<precio)return mostrarToast('Puntos insuficientes','warning');userActual.puntos-=precio;userActual.marco=id;await guardarTodo();actualizarUI();cargarPerfil();mostrarSeccionTienda('marcos');incrementarMision('tienda');mostrarToast('¡Marco comprado!','success');};
window.comprarColor=async function(id,precio){if(!userActual||userActual.puntos<precio)return mostrarToast('Puntos insuficientes','warning');userActual.puntos-=precio;userActual.colorNombre=id;await guardarTodo();actualizarUI();cargarPerfil();mostrarSeccionTienda('colores');incrementarMision('tienda');mostrarToast('¡Color comprado!','success');};
window.comprarFondo=async function(id,precio){if(!userActual||userActual.puntos<precio)return mostrarToast('Puntos insuficientes','warning');const fondo=fondosTienda.find(f=>f.id===id);userActual.puntos-=precio;userActual.fondo=fondo;await guardarTodo();actualizarUI();cargarPerfil();mostrarSeccionTienda('fondos');incrementarMision('tienda');mostrarToast('¡Fondo comprado!','success');};
window.comprarEfecto=async function(id,precio){if(!userActual||userActual.puntos<precio)return mostrarToast('Puntos insuficientes','warning');userActual.puntos-=precio;userActual.efecto=id;await guardarTodo();actualizarUI();cargarPerfil();mostrarSeccionTienda('efectos');incrementarMision('tienda');mostrarToast('¡Efecto comprado!','success');};
window.comprarMascota=async function(id,precio){if(!userActual||userActual.puntos<precio)return mostrarToast('Puntos insuficientes','warning');userActual.puntos-=precio;userActual.mascota=id;await guardarTodo();actualizarUI();actualizarMascota();mostrarSeccionTienda('mascotas');incrementarMision('tienda');mostrarToast('¡Mascota comprada!','success');};

// SELECTOR DE MATCHES ORDENADO
window.seleccionarMatchParaRegalo=function(id,precio){if(!userActual?.matches?.length){mostrarToast('Necesitas un Match','warning');return;}regaloPendienteId=id;regaloPendientePrecio=precio;const overlay=document.createElement('div');overlay.className='modal-overlay';overlay.id='modalSelectorOverlay';overlay.onclick=function(e){if(e.target===overlay)overlay.remove();};const mo=[...userActual.matches].sort((a,b)=>{const ua=usuarios.find(x=>x.uid===a);const ub=usuarios.find(x=>x.uid===b);return(ua?.user||'').localeCompare(ub?.user||'');});let mh=mo.map(uid=>{const u=usuarios.find(x=>x.uid===uid);if(!u)return'';const f=u.foto?`style="background-image:url(${u.foto});background-size:cover;"`:`style="background:${obtenerColorAleatorio(getInicial(u.user))};"`;return`<div class="match-option" onclick="window.enviarRegaloAMatch('${uid}')"><div class="avatar avatar-sm" ${f}>${!u.foto?getInicial(u.user):''}</div><div><b>${u.user}</b></div></div>`;}).join('');overlay.innerHTML=`<div class="modal-content"><h3>🎁 Enviar a...</h3>${mh}<button class="btn-sec" onclick="document.getElementById('modalSelectorOverlay').remove()" style="width:100%;margin-top:10px;">Cancelar</button></div>`;document.body.appendChild(overlay);};
window.enviarRegaloSorpresa=async function(){if(!chatActivo)return mostrarToast('Abre un chat primero','warning');if(userActual.puntos<50)return mostrarToast('Necesitas 50 puntos','warning');userActual.puntos-=50;const s=regalosSorpresa[Math.floor(Math.random()*regalosSorpresa.length)];const msg={de:userActual.user,texto:`__REGALO__${JSON.stringify({id:'sorpresa',icono:s.icono,nombre:s.nombre,color:s.color,animacion:'rebotar',esSorpresa:true})}`,fecha:new Date().toISOString()};if(!userActual.chats)userActual.chats={};if(!userActual.chats[chatActivo])userActual.chats[chatActivo]=[];userActual.chats[chatActivo].push(msg);const otro=usuarios.find(x=>x.uid===chatActivo);if(otro){if(!otro.chats)otro.chats={};if(!otro.chats[userActual.uid])otro.chats[userActual.uid]=[];otro.chats[userActual.uid].push(msg);await setDoc(doc(db,"usuarios",chatActivo),otro);}await guardarTodo();actualizarUI();incrementarMision('regalo');incrementarMision('tienda');cargarMensajes();mostrarToast(`¡${s.nombre} enviado!`,'success');};
window.enviarRegaloAMatch=async function(uid){if(!regaloPendienteId)return;const r=regalosTienda.find(r=>r.id===regaloPendienteId);if(!r||userActual.puntos<regaloPendientePrecio){mostrarToast('Puntos insuficientes','warning');document.getElementById('modalSelectorOverlay')?.remove();return;}userActual.puntos-=regaloPendientePrecio;userActual.regalosEnviados=(userActual.regalosEnviados||0)+1;const msg={de:userActual.user,texto:`__REGALO__${JSON.stringify({id:r.id,icono:r.icono,nombre:r.nombre,color:r.color,animacion:r.animacion})}`,fecha:new Date().toISOString()};if(!userActual.chats)userActual.chats={};if(!userActual.chats[uid])userActual.chats[uid]=[];userActual.chats[uid].push(msg);const otro=usuarios.find(x=>x.uid===uid);if(otro){if(!otro.chats)otro.chats={};if(!otro.chats[userActual.uid])otro.chats[userActual.uid]=[];otro.chats[userActual.uid].push(msg);await setDoc(doc(db,"usuarios",uid),otro);}await guardarTodo();actualizarUI();incrementarMision('regalo');incrementarMision('tienda');document.getElementById('modalSelectorOverlay')?.remove();document.getElementById('modalTienda').classList.add('hidden');if(chatActivo===uid)cargarMensajes();mostrarToast(`¡${r.nombre} enviado!`,'success');};
window.abrirEnviarRegalo=function(uid){chatActivo=uid;abrirTienda();mostrarSeccionTienda('regalos');};

// BLOQUEADOS
window.abrirModalBloqueados=function(){document.getElementById('menuPerfil')?.classList.add('hidden');cargarListaBloqueados();document.getElementById('modalBloqueados').classList.remove('hidden');};
window.cerrarModalBloqueados=function(){document.getElementById('modalBloqueados').classList.add('hidden');};
function cargarListaBloqueados(){const div=document.getElementById('listaBloqueados');if(!userActual?.bloqueados?.length){div.innerHTML='<p style="text-align:center;color:var(--text-secondary);">No tienes usuarios bloqueados</p>';return;}div.innerHTML=userActual.bloqueados.map(uid=>{const u=usuarios.find(x=>x.uid===uid);if(!u)return'';const f=u.foto?`style="background-image:url(${u.foto});background-size:cover;"`:`style="background:${obtenerColorAleatorio(getInicial(u.user))};"`;return`<div class="match-card" style="justify-content:space-between;"><div style="display:flex;align-items:center;gap:8px;"><div class="avatar avatar-xs" ${f}>${!u.foto?getInicial(u.user):''}</div><b>${u.user}</b></div><button onclick="window.desbloquearUsuario('${uid}')" style="width:auto;padding:5px 10px;font-size:11px;min-height:auto;background:var(--primary);color:white;border-radius:16px;">Desbloquear</button></div>`;}).join('');feather.replace();}
window.desbloquearUsuario=async function(uid){if(!userActual?.bloqueados)return;userActual.bloqueados=userActual.bloqueados.filter(x=>x!==uid);await guardarTodo();cargarListaBloqueados();mostrarToast('Usuario desbloqueado','info');};
